#!/bin/bash

set -euo pipefail

PI_BIN="$(command -v pi || true)"
if [[ -z "${PI_BIN}" ]]; then
  echo "pi not found. PATH=${PATH}" >&2
  exit 127
fi

"${PI_BIN}" update
"${PI_BIN}" update --extensions
